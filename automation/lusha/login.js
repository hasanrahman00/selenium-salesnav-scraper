const loginLusha = async (driver, cookies) => {
  if (!driver) {
    return;
  }
  if (!Array.isArray(cookies)) {
    return;
  }
  // TODO: implement login flow using cookies.
};

module.exports = {
  loginLusha,
};
